# This file is generated by `cargo hakari`.
# To regenerate, run:
#     cargo hakari generate

[package]
name = "lethe-workspace-hack"
version = "0.1.0"
edition.workspace = true
description = "workspace-hack package, managed by hakari"
# You can choose to publish this crate: see https://docs.rs/cargo-hakari/latest/cargo_hakari/publishing.
publish = false

# The parts of the file between the BEGIN HAKARI SECTION and END HAKARI SECTION comments
# are managed by hakari.

### BEGIN HAKARI SECTION
[dependencies]
chrono = { version = "0.4", default-features = false, features = ["arbitrary", "now", "serde"] }
color-eyre = { version = "0.6" }
num-traits = { version = "0.2", default-features = false, features = ["std"] }
serde = { version = "1", default-features = false, features = ["derive", "std"] }
serde_core = { version = "1", default-features = false, features = ["alloc", "result", "std"] }
uuid = { version = "1", default-features = false, features = ["arbitrary", "serde", "std", "v4"] }

[build-dependencies]
proc-macro2 = { version = "1" }
quote = { version = "1" }
syn = { version = "2", features = ["extra-traits", "full", "visit"] }

### END HAKARI SECTION
