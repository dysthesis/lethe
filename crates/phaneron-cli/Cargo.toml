[package]
name = "phaneron-cli"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
phaneron-core = { path = "../phaneron-core" }
phaneron-workspace-hack = { version = "0.1", path = "../phaneron-workspace-hack" }
